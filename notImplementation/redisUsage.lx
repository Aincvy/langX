ref langX.rt.util.redis;

redis = new Redis("127.0.0.1", 6379);
// redis.connect("127.0.0.1", 6379)；

redis.put("keyStr", "hello");
redis.put("keyInt", 1) ;

a = redis.get("keyStr");
a = redis.get("keyInt");

redis.cmd("hset a a 1");

// 以下为展开类的一些思考， 不属于 redis 库的范畴

set ref redis {
	b = cmd("set keyStr abc") ;
	b = get("keyStr") ;
}
